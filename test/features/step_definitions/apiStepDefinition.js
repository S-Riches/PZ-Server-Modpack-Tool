import assert from "assert";
import { Given, When, Then } from "@cucumber/cucumber";

Given("i have my collection id", function () {
    // Write code here that turns the phrase above into concrete actions
    return "pending";
});

When("I give a valid collection Id", function () {
    // Write code here that turns the phrase above into concrete actions
    return "pending";
});

Then(
    "the function returns a Json response with an error code of {int}",
    function (int) {
        // Then('the function returns a Json response with an error code of {float}', function (float) {
        // Write code here that turns the phrase above into concrete actions
        return "pending";
    }
);
